name: quiz_host_app
description: Quiz App - Host Dashboard
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management — BLoC
  flutter_bloc: ^8.1.4
  bloc: ^8.1.3

  # Comparaison des states
  equatable: ^2.0.5

  # Navigation
  go_router: ^13.2.0
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  graphql_flutter: ^5.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true